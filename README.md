## Portfolio (React + TypeScript + Vite + Ant Design)

### Setup

```bash
corepack pnpm install
corepack pnpm dev
```

### Notes

- Theme toggle is implemented via `ConfigProvider` + a small typed theme context.
- Architecture is organized by layers: `app/`, `pages/`, `widgets/`, `features/`, `shared/`.

